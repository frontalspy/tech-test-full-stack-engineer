describe("Test Leads", () => {
  describe("Test render", () => {
    it("Should render invited and accepted tabs", () => {});
    it("Should render invited tab with 2 jobs", () => {});
  });
  describe("Test interaction", () => {
    it("Should move job to accepted tab on accept action", () => {});
    it("Should remove job to on decline action", () => {});
  });
});
